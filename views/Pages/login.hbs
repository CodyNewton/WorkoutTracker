{{#> layout name="main" }}
{{> message}}

<div class="container d-flex justify-content-center align-items-center" style="min-height: 90vh;">
  <div class="card bg-black text-light border border-secondary shadow-lg px-4 py-5" style="max-width: 420px; width: 100%;">
    
    <div class="text-center mb-4">
      <i class="bi bi-person-circle display-3 text-secondary"></i>
      <h2 class="mt-2 fw-semibold">Welcome Back</h2>
      <p class="text-muted">Log in to track your workouts</p>
    </div>

    <form action="/login" method="POST" class="needs-validation" novalidate>
      <div class="mb-3">
        <label for="username" class="form-label text-uppercase small">Username</label>
        <input 
          type="text" 
          class="form-control bg-dark text-light border-secondary rounded-1 shadow-sm" 
          id="username" 
          name="username" 
          required
        >
      </div>

      <div class="mb-4">
        <label for="password" class="form-label text-uppercase small">Password</label>
        <input 
          type="password" 
          class="form-control bg-dark text-light border-secondary rounded-1 shadow-sm" 
          id="password" 
          name="password" 
          required
        >
      </div>

      <div class="d-grid mb-3">
        <button type="submit" class="btn btn-secondary btn-lg rounded-1">
          <i class="bi bi-box-arrow-in-right me-2"></i> Login
        </button>
      </div>
    </form>

    <div class="text-center mt-2">
      <small class="text-muted">Don't have an account? <a href="/register" class="text-info text-decoration-none">Register</a></small>
    </div>
  </div>
</div>

{{/layout}}
